- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.config/nvim: 
    ~/.tmux.conf:
      path: tmux/.tmux.conf

- shell:
  - [git submodule update --init --recursive, Installing submodules]
