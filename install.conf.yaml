- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.config/nvim: 
    ~/.config/wezterm:
    ~/.config/fish:
    ~/.tmux.conf:
      path: tmux/.tmux.conf
    ~/.alacritty.yml:
      path: alacritty.yml
    ~/.config/zellij:
    ~/.local/bin/scripts:
      path: scripts/

- shell:
  - [git submodule update --init --recursive, Installing submodules]
